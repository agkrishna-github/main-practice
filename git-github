git --version

https://git-scm.com/downloads

in git bash..

ls

clear

pwd

git config --global user.name "my name"
git config --global user.email "my@gmail.com"
git config --list

git clone <repository link>

ls -a

git status

untracked
modified
staged
unmodified

git add <file name>

git commit -m 'some message'

git add .

git push origin main/master

...

error: 403

solution:

go to control panel -> user accounts -> credential manager -> manage windows credential -> 

under that:





....

to create new git repo:

git init

git remote add origin <link>

git remote -v

git branch

git branch -M main - to change the branch name from master to main

git push origin main

git push -u origin main

...

git branch - to check branch

git branch -M main - to rename branch

git checkout <branch name> - to navigate

git checkout -b <branch name> to create new branch

git branch -d <branch name> to delete branch

....

git push origin anotherbranch

.......

merging code:

git diff <branch name>

git merge <branch name> 

...

merging code way 2:

create pull request - PR

..

git pull origin <branch name>

..........

undoing changes:

1 staged changes

  git reset <filename>

  git reset

2 commited changes for one commit
  
  git reset HEAD~1

3 commited changes for many commits

  git reset <commit hash>

  git reset --hard <commit hash>

.....

git log













